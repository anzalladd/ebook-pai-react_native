import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  wrapper: {
    marginTop: 5,
  },
  pilihan: {
    display: 'flex',
    flexDirection: 'row',
    alignItems: 'center',
    marginTop: 20,
  },
  textPilihan: {
    marginLeft: 14,
  },
});

export default styles;
